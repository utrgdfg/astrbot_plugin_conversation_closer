# 更新日志

本项目的所有重要变更均遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## 尚未发布

### 修复

- 历史窗口已经淘汰旧消息时，不再把剩余局部问答误认为完整上下文。
- 消息 ID 被适配器重复使用但正文不同时，不再复用旧判断结果。
- 尊重 AstrBot 的私聊唤醒设置，未准备进入主模型的消息不会调用对话判断模型。
- 媒体、未知消息和被截断内容不再作为高可信度 `END` 的完整依据。
- 判断超时后立即自动放行，迟到的模型结果不会重新参与拦截。
- 发送后钩子重复触发时，机器人回复只记录一次。

### 安全与质量

- 增加判断模型输出的本地字符上限，并补充取消、去重、媒体和历史淘汰回归测试。
- 修正非斜杠指令组的识别，以及发布清单中过期的固定版本号。
- 将 `/closer` 管理命令限制为 AstrBot 管理员，并限制同时挂起的判断调用数量。
- 即使模型受提示词注入影响返回 `END`，带明确问号的用户消息也会自动放行。
- 引用旧消息不会再被静默忽略；识别到提示词注入信号时只允许保守放行。
- 成功确认一次自然闭环后开始新的会话阶段，避免旧的淘汰标记让后续收尾永久失效。
- 移除 README 第三方访问计数器，避免文档渲染时产生非必要的外部请求。

## [0.2.2] - 2026-08-22

### 优化

- 强化对“局部问答完成”和“整个对话任务完成”的区分，优先放行仍在收集信息、等待授权或执行中间步骤的交流。
- 上下文缺失、内容截断或相邻角色不足以证明闭环时，将模型给出的 `END` 保守降级为 `UNCERTAIN`。
- 加固聊天数据边界，避免提示词注入伪造上下文完整性或提前闭合数据区域。

### 质量

- 扩充版本号、错误码、配置值、地点、时间、数量、执行授权和多步骤任务等安全关键回归场景。
- 明确要求安全关键测试集中预期 `END` 数量为零，并验证程序护栏不会把其他结果升级为 `END`。

## [0.2.1] - 2026-08-20

### 变更

- 将更新日志、贡献指南、安全策略、资源授权、发布清单和测试语料说明改为中文。
- 进一步中文化 README、项目包描述、持续集成展示名称、插件运行日志和开发检查提示。

## [0.2.0] - 2026-08-20

### 变更

- 简化插件展示名称和面向用户的术语。
- 将管理面板配置整理为基本开关、判断设置、对话记录和高级设置四个分区。
- 增加带版本标记的一次性配置迁移：保留旧版平铺配置，同时防止兼容值覆盖用户之后在管理面板中保存的新设置。
- 简化管理命令输出，并将可点击的 Mayu 萌娘计数器移到 README 末尾。

### 质量

- 增加配置分组、旧配置迁移、管理面板布局和 README 的回归测试。
- 扩展 AstrBot 源码契约检查，覆盖分组对象配置格式。

## [0.1.0] - 2026-08-16

### 新增

- 提供 `END`、`CONTINUE`、`UNCERTAIN` 三态对话判断，并严格校验 JSON 输出。
- 对模型未配置、超时、返回格式错误和插件内部异常实行自动放行。
- 提供会话隔离的有界历史、消息去重、每会话独立锁和超时清理。
- 在主聊天模型调用前静默拦截，并在消息发送后记录机器人回复；同时提供 `/closer` 管理命令。
- 提供隐私说明、回归测试语料、自动化测试、代码检查和安全检查。
